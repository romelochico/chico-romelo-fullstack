import type { StyledComponent } from 'styled-components'
import type { DefaultTheme } from 'styled-components'

export const HeroWrapper: StyledComponent<'header', DefaultTheme>
export const HeroBg: StyledComponent<'div', DefaultTheme, { $imagePos?: string }>
export const HeroContent: StyledComponent<'div', DefaultTheme>
export const HeroLabel: StyledComponent<'div', DefaultTheme>
export const HeroH1: StyledComponent<'h1', DefaultTheme>
